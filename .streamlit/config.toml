[theme]
primaryColor = "#4F46E5"
backgroundColor = "#F7F6FF"
secondaryBackgroundColor = "#EEECFC"
textColor = "#1F2D4E"
font = "sans serif"

[server]
# Bind to all interfaces so other machines on the office LAN can reach it.
# (In production run with: streamlit run app.py --server.address 0.0.0.0)
headless = true
